{{!< default}}

<header>

  <section class="row board">
    <div class="col-single relative">
      {{!-- <h1 class="page-title">{{tag.name}}</h1> --}}
      {{!-- <i class="icon icon-tag"></i> --}}
      <h1 class="title-focus">{{tag.name}}</h1>
    </div>
    <div class="col-double">
      <div class="col-full">
        {{#if tag.description}}
        <p class="focus">
          {{tag.description}}
        </p>
        {{/if}}
      </div>
    </div>
  </section>


</header>

{{! The main content area on the homepage }}
<main class="content tag-page" role="main">

  {{! The tag below includes the post loop - partials/loop.hbs }}
  {{> "loop"}}

</main>
